const React = require('react');

const di = require('./DataInterface');

const Results = function() {
    return (
      <div>
        <h1>Results Page</h1>
      </div>
    );
  }
  
  module.exports = Results;